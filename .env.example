# ============================================================================
# SPECSNET - Configuración de Entorno
# ============================================================================
# Copiar a .env y ajustar los valores según tu infraestructura

# ----------------------------------------------------------------------------
# SEGURIDAD
# ----------------------------------------------------------------------------

# Secreto compartido para autenticación cliente-servidor
# Generar con: python -c "import secrets; print(secrets.token_hex(32))"
SHARED_SECRET=CHANGE_ME_TO_RANDOM_TOKEN

# Subnets permitidas (whitelist de redes, separadas por comas)
# Formato: IP/CIDR,IP/CIDR,...
ALLOWED_SUBNETS=10.0.0.0/8,10.100.0.0/16,127.0.0.1/32,192.168.1.0/24
# Límites de seguridad
MAX_BUFFER_SIZE=10485760
MAX_CONNECTIONS_PER_IP=3
CONNECTION_TIMEOUT=30
MAX_FIELD_LENGTH=1024

# ----------------------------------------------------------------------------
# RED Y PUERTOS
# ----------------------------------------------------------------------------

# Puerto TCP del servidor (recepción de datos de clientes)
SERVER_PORT=5255


# ----------------------------------------------------------------------------
# BASE DE DATOS
# ----------------------------------------------------------------------------

# Ruta de la base de datos SQLite (relativa a la raíz del proyecto)
# Para desarrollo: data/specs.db
# Para producción empaquetada: specs.db (junto al ejecutable)
DB_PATH=sql/specs.db

# ----------------------------------------------------------------------------
# ESCANEO DE RED
# ----------------------------------------------------------------------------

# Timeouts de escaneo (en segundos)
PING_TIMEOUT=1.0
SCAN_PER_HOST_TIMEOUT=0.8
SCAN_PER_SUBNET_TIMEOUT=8.0
SCAN_PROBE_TIMEOUT=0.9

# Tamaño de batch para pings paralelos
PING_BATCH_SIZE=50

# ----------------------------------------------------------------------------
# RUTAS DE SALIDA
# ----------------------------------------------------------------------------

# Directorio para archivos temporales (CSVs, JSONs, logs)
OUTPUT_DIR=output

# Directorio para archivos de diagnóstico DirectX
DXDIAG_OUTPUT_DIR=output

# ----------------------------------------------------------------------------
# INSTRUCCIONES DE CONFIGURACIÓN
# ----------------------------------------------------------------------------
# 1. Copiar este archivo: cp .env.example .env (Windows: copy .env.example .env)
# 2. Generar secreto aleatorio y reemplazar SHARED_SECRET
# 3. Ajustar ALLOWED_SUBNETS según las redes de tu organización
# 4. Configurar rangos de escaneo (SCAN_SUBNET_START/END)
# 5. El archivo .env NO se sube a Git (protegido por .gitignore)
# ============================================================================